# `blocks-pro`

Pro **block** layer. The installable package is **`packages/blocks-pro/core`** (`@blockera/blocks-pro-core`).

Read [core/README.md](core/README.md) for APIs, tests, and how it pairs with free `@blockera/blocks-core`.

Do not add a second npm package under this folder without a `package.json` and README in that package directory.
