// @flow

/**
 * Global-style item caps and shade editing unlock via `@blockera/products`
 * when `blockera-pro` has a valid `meta.license`. Kept as a no-op export.
 *
 * @return {void}
 */
export const unlockGlobalStyles = () => {};
